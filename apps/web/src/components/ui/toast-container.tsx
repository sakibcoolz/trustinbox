'use client';

// DEPRECATED: This component is superseded by the ToastProvider in toast-context.tsx
// which renders its own toast UI. Kept for backwards compatibility.
// The ToastProvider in providers.tsx already renders toasts.
export function ToastContainer() {
  return null;
}
